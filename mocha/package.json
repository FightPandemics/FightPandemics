{
  "name": "mocha",
  "version": "1.0.0",
  "description": "Mocha API integration tests",
  "scripts": {
    "test": "mocha './test/**/*.js' --recursive --timeout 60000 --exit"
  },
  "private": true,
  "devDependencies": {
    "chai": "^4.2.0",
    "http-status": "^1.4.2",
    "lodash": "^4.17.20",
    "mocha": "^8.1.0",
    "mongoose": "^5.10.13",
    "should": "^13.2.3",
    "supertest": "^4.0.2"
  }
}
