{
  "name": "mocha",
  "version": "1.0.0",
  "description": "Mocha API integration tests",
  "scripts": {
    "test": "mocha './mocha/test/**/*.js' --recursive --timeout 60000 --exit"
  },
  "private": true,
  "devDependencies": {
    "mocha": "^8.1.0"
  }
}