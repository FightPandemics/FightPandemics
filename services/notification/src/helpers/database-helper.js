class DatabaseHelper {
  constructor(config) {
    // TODO add Mongoose client
    // this.db =
  }

  async findUnreadNotifications() {
    // TODO query for unread notifications older than 5 minutes. Group by user ID.
  }
}

module.exports = {
  DatabaseHelper,
}
