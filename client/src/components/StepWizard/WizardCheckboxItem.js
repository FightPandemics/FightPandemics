import styled from "styled-components";
import { Checkbox } from "antd-mobile";
const CheckboxItem = Checkbox.CheckboxItem;

export default styled(CheckboxItem)`
  .am-checkbox-inner {
    border-radius: 0;
  }
`;
